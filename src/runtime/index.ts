/**
 * If you're seeing this it means that you have not generated Nexus Prisma CRUD.
 *
 * Nexus Prisma CRUD is partly a generated library. Before using it you must run its generation step.
 *
 */

import { GeneratedTypes } from "../generator/types"

export const generatedTypes: GeneratedTypes = {}
